# — Environment for Gemma 4 integration —
# Copy to .env and fill in the tunnel URL provided by the team.
# The tunnel URL must NEVER be committed to the repo.

GEMMA_BASE_URL=
GEMMA_API_KEY=x
GEMMA_TIMEOUT=60

# Optional: force a specific model variant
# GEMMA_MODEL=gemma4:e2b-it-qat
